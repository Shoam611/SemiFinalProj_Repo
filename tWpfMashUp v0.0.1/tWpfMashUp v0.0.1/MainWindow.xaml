<Window x:Class="tWpfMashUp_v0._0._1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tWpfMashUp_v0._0._1"
        mc:Ignorable="d" 
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        WindowStyle="SingleBorderWindow"
        MinWidth="300" MinHeight="500"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"         
        DataContext="{Binding Source={StaticResource Locator},Path=Main}"
        Height="500" Width="600">

    <!--<i:Interaction.Triggers>
        AllowsTransparency="True"
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding OnLoadedCommand}" PassEventArgsToCommand="True"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>-->
    <Grid  Background="Transparent">
        <ContentControl Content="{Binding View}" Width="auto" Height="auto" />
    </Grid>
</Window>
